#include "game.h"
#include <stdio.h>


void test(void) {

/*
	Tile rowTest[NUM_COL][NUM_ROWS];

	//printf("%ld\n\n", sizeof(rowTest));

	for (int i = 0; i < NUM_MINES; i++) {

			int x, y;
			x = rand() % NUM_COL;
			y = rand() % NUM_ROWS;

			rowTest[x][y].isMine = true;

	}

	/*

	printf("%d\n\n", rand() % NUM_ROWS);
	printf("%d\n\n", rand() % NUM_ROWS);
	printf("%d\n\n", rand() % NUM_ROWS);*/

}
